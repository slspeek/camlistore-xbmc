#!/bin/bash


zip -r script.module.camlistore.zip script.module.camlistore/
zip -r  plugin.audio.camlistore.zip plugin.audio.camlistore/
zip -r plugin.video.camlistore.zip plugin.video.camlistore/
zip -r plugin.image.camlistore.zip plugin.image.camlistore/
